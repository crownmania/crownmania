{
    "name": "crownmania-smart-contracts",
    "version": "1.0.0",
    "description": "CrownMania ERC-721 smart contracts for Polygon",
    "scripts": {
        "compile": "npx hardhat compile",
        "test": "npx hardhat test",
        "deploy:amoy": "npx hardhat run scripts/deploy.js --network amoy",
        "deploy:polygon": "npx hardhat run scripts/deploy.js --network polygon",
        "deploy:local": "npx hardhat run scripts/deploy.js --network hardhat"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "@openzeppelin/contracts": "^5.0.0",
        "dotenv": "^16.3.1",
        "hardhat": "^2.19.0"
    }
}